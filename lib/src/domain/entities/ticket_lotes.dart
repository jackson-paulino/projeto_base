import 'lote_ticket.dart';

class TicketLotes {
  String? name;
  String? type;
  bool? useCard;
  LoteTicket? lote;

  TicketLotes({
    this.name,
    this.type,
    this.useCard,
    this.lote,
  });
}
